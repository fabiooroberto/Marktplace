# netcore-authentication-keycloak

## Subir o keycloak no docker
- abrir terminal na mesma pasta que se encontra o docker-compose.yml
- executar no terminal: docker-compose up -d

- abrir no navegador http://localhost:8080 
Colocar as credenciais que estão no arquivo docker-compose